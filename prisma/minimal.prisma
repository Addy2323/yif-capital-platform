generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:myamba2323@localhost:5432/yifdb?schema=public"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
